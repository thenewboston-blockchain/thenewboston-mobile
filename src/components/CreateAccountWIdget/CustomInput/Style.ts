import {StyleSheet} from "react-native";

const Styles = StyleSheet.create({
    labelStyles: {
        textTransform: "uppercase",
        color: "#62737E",
        zIndex: 100,
        fontWeight: "bold",
        fontSize: 10
    },
    containerStyles: {
        borderWidth: 0.3,
        paddingHorizontal: 10,
        backgroundColor: "rgba(30, 50, 58, 0.6)",
        borderColor: "#2B4150",
        borderRadius: 12,
        zIndex: 1,
        height: 55,
        marginBottom: 20
    },
    inputStyles: {
        fontSize: 12
    }
});

export default Styles;
